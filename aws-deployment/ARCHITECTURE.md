# 🏗️ AWS Deployment Architecture

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         Internet                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTPS/HTTP
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    AWS EC2 Instance                          │
│                  (Ubuntu 22.04 LTS)                          │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │              Nginx (Optional)                       │    │
│  │         Reverse Proxy + SSL                         │    │
│  │              Port 80/443                            │    │
│  └──────────────────────┬─────────────────────────────┘    │
│                         │                                    │
│                         │ Port 8000                          │
│                         │                                    │
│  ┌──────────────────────▼─────────────────────────────┐    │
│  │         Docker Container                            │    │
│  │      brainwave-backend-prod                         │    │
│  │                                                      │    │
│  │  ┌────────────────────────────────────────────┐   │    │
│  │  │     FastAPI Application                     │   │    │
│  │  │     (Python 3.11)                           │   │    │
│  │  │                                             │   │    │
│  │  │  • AI Chat Agent                            │   │    │
│  │  │  • Learning Path Engine                     │   │    │
│  │  │  • Progress Tracking                        │   │    │
│  │  │  • Flashcard System                         │   │    │
│  │  │  • Quiz Generator                           │   │    │
│  │  │  • Media Processing                         │   │    │
│  │  └────────────────────────────────────────────┘   │    │
│  │                                                      │    │
│  │  ┌────────────────────────────────────────────┐   │    │
│  │  │     Data Storage (Volumes)                  │   │    │
│  │  │                                             │   │    │
│  │  │  • SQLite Database                          │   │    │
│  │  │  • ChromaDB (Vector Store)                  │   │    │
│  │  │  • Uploaded Media Files                     │   │    │
│  │  │  • Generated Exports                        │   │    │
│  │  │  • Cache Files                              │   │    │
│  │  └────────────────────────────────────────────┘   │    │
│  └──────────────────────────────────────────────────┘    │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │         External Services (via API)              │      │
│  │                                                   │      │
│  │  • Google Gemini AI                              │      │
│  │  • AssemblyAI (Transcription)                    │      │
│  │  • YouTube (yt-dlp)                              │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
         │
         │ Elastic IP (Static)
         │ YOUR_ELASTIC_IP
         │
```

## Network Flow

```
User Request Flow:
─────────────────

1. User/Frontend → http://YOUR_ELASTIC_IP:8000/api/...
                    │
2. AWS Security Group (Firewall)
   ├─ Port 22:  SSH (Your IP only)
   ├─ Port 80:  HTTP (0.0.0.0/0)
   ├─ Port 443: HTTPS (0.0.0.0/0)
   └─ Port 8000: Backend API (0.0.0.0/0)
                    │
3. [Optional] Nginx Reverse Proxy
   ├─ SSL Termination
   ├─ Request Buffering
   └─ Load Balancing (future)
                    │
4. Docker Container (brainwave-backend-prod)
   └─ FastAPI Application
      ├─ Route Handling
      ├─ AI Processing
      ├─ Database Operations
      └─ Response Generation
                    │
5. Response → User/Frontend
```

## Data Flow

```
┌──────────────┐
│   Frontend   │
│  (React App) │
└──────┬───────┘
       │
       │ HTTP/HTTPS
       │
┌──────▼────────────────────────────────────────┐
│           Backend API                          │
│                                                │
│  ┌──────────────────────────────────────┐    │
│  │  Authentication & Authorization       │    │
│  └──────────────┬───────────────────────┘    │
│                 │                              │
│  ┌──────────────▼───────────────────────┐    │
│  │  Business Logic Layer                 │    │
│  │  • AI Agents                          │    │
│  │  • Learning Engine                    │    │
│  │  • Content Processing                 │    │
│  └──────────────┬───────────────────────┘    │
│                 │                              │
│  ┌──────────────▼───────────────────────┐    │
│  │  Data Access Layer                    │    │
│  │  • SQLAlchemy ORM                     │    │
│  │  • ChromaDB Client                    │    │
│  │  • File System                        │    │
│  └──────────────┬───────────────────────┘    │
│                 │                              │
└─────────────────┼──────────────────────────────┘
                  │
       ┌──────────┴──────────┐
       │                     │
┌──────▼────────┐   ┌───────▼────────┐
│  SQLite DB    │   │  ChromaDB      │
│  (User Data)  │   │  (Vectors)     │
└───────────────┘   └────────────────┘
```

## Deployment Flow

```
┌─────────────────┐
│  GitHub Repo    │
│  (Your Code)    │
└────────┬────────┘
         │
         │ git clone/pull
         │
┌────────▼────────────────────────────────────┐
│  EC2 Instance                               │
│                                             │
│  1. deploy.sh runs                          │
│     ├─ Installs Docker                      │
│     ├─ Installs Docker Compose              │
│     └─ Clones repository                    │
│                                             │
│  2. Docker Build                            │
│     ├─ Pulls Python 3.11 base image         │
│     ├─ Installs system dependencies         │
│     ├─ Installs Python packages             │
│     └─ Copies application code              │
│                                             │
│  3. Docker Compose Up                       │
│     ├─ Creates volumes                      │
│     ├─ Starts container                     │
│     └─ Runs health checks                   │
│                                             │
│  4. Application Running                     │
│     └─ Backend API available on port 8000   │
└─────────────────────────────────────────────┘
```

## Storage Architecture

```
EC2 Instance Storage:
────────────────────

/home/ubuntu/brainwave-backend/          (Git Repository)
├── backend/
│   ├── main.py                          (Application Entry)
│   ├── .env.production                  (Configuration)
│   ├── brainwave_tutor.db              (SQLite Database)
│   └── ...

Docker Volumes (Persistent):
────────────────────────────

backend_uploads/                         (User Uploads)
├── media/                               (Audio/Video files)
└── documents/                           (PDFs, Docs)

backend_chroma/                          (Vector Database)
└── chroma.sqlite3                       (ChromaDB data)

backend_exports/                         (Generated Files)
└── backups/                             (Database backups)

backend_cache/                           (Temporary Cache)
└── transcripts/                         (Cached transcriptions)
```

## Security Layers

```
┌─────────────────────────────────────────────┐
│  Layer 1: AWS Security Group                │
│  • Firewall rules                           │
│  • Port restrictions                        │
│  • IP whitelisting (SSH)                    │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│  Layer 2: Nginx (Optional)                  │
│  • SSL/TLS encryption                       │
│  • Rate limiting                            │
│  • Request filtering                        │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│  Layer 3: Application                       │
│  • JWT authentication                       │
│  • API key validation                       │
│  • Input validation                         │
│  • CORS policies                            │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│  Layer 4: Data                              │
│  • Encrypted environment variables          │
│  • Secure file permissions                  │
│  • Database access control                  │
└─────────────────────────────────────────────┘
```

## Scaling Options (Future)

```
Current Setup (Single Instance):
────────────────────────────────
Frontend → EC2 Instance → Backend Container


Future: Load Balanced (Multiple Instances):
───────────────────────────────────────────
                    ┌─ EC2 Instance 1 → Backend
Frontend → ALB ─────┼─ EC2 Instance 2 → Backend
                    └─ EC2 Instance 3 → Backend
                              │
                    ┌─────────▼──────────┐
                    │   RDS (PostgreSQL) │
                    │   S3 (Media Files) │
                    └────────────────────┘


Future: Containerized (ECS/EKS):
────────────────────────────────
Frontend → CloudFront → ALB → ECS/EKS Cluster
                                    │
                          ┌─────────┼─────────┐
                          │         │         │
                       Task 1    Task 2    Task 3
                          │         │         │
                          └─────────┼─────────┘
                                    │
                          ┌─────────▼──────────┐
                          │   RDS + S3 + Redis │
                          └────────────────────┘
```

## Cost Breakdown

```
Monthly Costs (t3.medium):
─────────────────────────

EC2 Instance (t3.medium)     $30-35
├─ 2 vCPUs
├─ 4 GB RAM
└─ On-demand pricing

EBS Storage (30GB gp3)       $3
├─ Database
├─ Application files
└─ Uploads

Elastic IP                   $0
└─ Free when attached

Data Transfer                $0-5
├─ First 1GB free
└─ $0.09/GB after

─────────────────────────────
Total:                       $33-43/month


Optional Add-ons:
────────────────
RDS (db.t3.micro)           +$15/month
S3 Storage                  +$0.023/GB
CloudWatch Logs             +$0.50/GB
Route 53 (DNS)              +$0.50/month
```

## Monitoring Points

```
Application Health:
──────────────────
✓ /api/health endpoint
✓ Container status (docker ps)
✓ Application logs
✓ Response times

System Health:
─────────────
✓ CPU usage (htop)
✓ Memory usage (free -h)
✓ Disk space (df -h)
✓ Network traffic

Docker Health:
─────────────
✓ Container uptime
✓ Container restarts
✓ Volume usage
✓ Image size
```

This architecture provides a solid foundation that can scale as your user base grows!
